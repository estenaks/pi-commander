# Copy this file to secrets.py on your Pico (NOT in the repo) and fill in your values.
# secrets.py is gitignored — never commit it.

WIFI_SSID = "your-ssid"
WIFI_PASS  = "your-password"
SERVER     = "http://192.168.1.xx"  # Your Pi's LAN IP — avoid .local on MicroPython